<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>WMDB</title>
  <!-- load local stylesheet (css) -->
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <form class="search" method="GET" action="/query">
      <p><label>search: <input type="text" required name="query"></label>
      <label>kind
              <select required name="kind">
                  <option value="">choose one</option>
                  <option value="person">person name</option>
                  <option value="movie">movie title</option>
              </select>
      </label>
      <input type="submit" value="search"></p>
  </form>

  <h1 class="person"><%= person.name %></h1>

  <p><%= person.name %>, added by <%= person.addedby.name %>, was born on <%= person.birthdate %>.</p>

  <p>Appeared in: </p>
  <ul>
    <% for (var i=0; i < person.movies.length; i++) {%>
      <li>
      <a href="../tt/<%= person.movies[i].tt %>"><%= person.movies[i].title %> (<%= person.movies[i].release%>)</a>
      </li>
    <% } %>
  </ul>

  <script src="/main.js"></script>
</body>
</html>